{
  "name": "webreed-text-mode",
  "version": "0.0.4",
  "description": "Text mode plugin for webreed.",
  "repository": {
    "type": "git",
    "url": "https://github.com/webreed/webreed-text-mode"
  },
  "main": "lib/setup.js",
  "typings": "lib/setup.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "npm run clean && tsc --pretty",
    "clean": "rm -rf lib",
    "test": "mocha ./test/unit/ --recursive"
  },
  "author": "Rotorz Limited",
  "license": "MIT",
  "dependencies": {
    "fs-promise": "^0.5.0",
    "js-yaml": "^3.4.6",
    "webreed-core": "^0.0.x"
  },
  "devDependencies": {
    "merge2": "^1.0.1",
    "mocha": "^2.3.4",
    "mocha-testdata": "^1.1.2",
    "should": "^8.0.2",
    "typescript": "^1.8.10"
  }
}
